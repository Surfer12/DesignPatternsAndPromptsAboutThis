package Patterns; // Added package declaration

/**
 * Decorator Pattern implementation for a configurable logging system
 */
public interface Logger { // Added Logger interface definition
    void log(String message);
}
